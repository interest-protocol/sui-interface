import { Box, Typography } from '@interest-protocol/ui-kit';
import { FC } from 'react';

const BorrowMarketTable: FC = () => (
  <Box>
    <Typography variant="small">Borrow Market</Typography>
    <Box></Box>
  </Box>
);

export default BorrowMarketTable;
